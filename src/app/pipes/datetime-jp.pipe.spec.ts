import { DatetimeJpPipe } from './datetime-jp.pipe';

describe('DatetimeJpPipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeJpPipe();
    expect(pipe).toBeTruthy();
  });
});
